# Dictionary
App takes user input and attempts to look up the word in a dictionary list.
Dictionary is imported from data.json.
If user input is not found, app will suggest words. If matching word is not found,
app will prompt user to try again.
